name: Keep Alive (Backup 2)

on:
  schedule:
    # Offset +7 min from primary — covers gaps in GitHub cron scheduling
    - cron: '7,17,27,37,47,57 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: curl -sf https://zeroclaw-agent.onrender.com/health -o /dev/null -w "%{http_code}\n"
