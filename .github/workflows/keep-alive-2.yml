name: Keep Alive (Backup 1)

on:
  schedule:
    # Offset +3 min from primary — covers gaps in GitHub cron scheduling
    - cron: '3,13,23,33,43,53 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: curl -sf https://zeroclaw-agent.onrender.com/health -o /dev/null -w "%{http_code}\n"
